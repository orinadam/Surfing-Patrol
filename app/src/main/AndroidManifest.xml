<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.surfingpatrol">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SurfingPatrol">
        <activity android:name=".AddImage"></activity>
        <activity android:name=".Gallery">


        </activity>
        <activity android:name=".WindScreen" />
        <activity android:name=".SurfingSpots"></activity>
        <activity android:name=".Register" />
        <activity android:name=".WavesScreen" />
        <activity android:name=".TemperatureScreen" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

</manifest>